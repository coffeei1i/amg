# Asset ledger

No external images, logos, icons or web fonts are embedded in the final
workflow. Every diagram element is an editable draw.io primitive. The SVG and
PNG files are derived release exports from the same content contract.
